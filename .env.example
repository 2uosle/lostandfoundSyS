# Database connection
DATABASE_URL="postgresql://user:password@localhost:5432/lostfound?schema=public"

# NextAuth configuration
NEXTAUTH_URL="http://localhost:3000"
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=""

# Google OAuth
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Environment
NODE_ENV="development"

# Cron job authentication (for rate limit cleanup, etc.)
# Generate with: openssl rand -base64 32
CRON_SECRET=""

# SMTP (optional, required for sending emails)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="username"
SMTP_PASS="password"
SMTP_FROM="Lost & Found <no-reply@example.com>"
